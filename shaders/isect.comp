#version 450

layout(binding = 0) buffer Values {
   int values[];
};

layout (push_constant) uniform PushConstants {
	uint N;
};

layout (local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

void main() {
	uint index = gl_GlobalInvocationID.x;
	if (index >= N) 
		return;	

	values[index] += 1;
}